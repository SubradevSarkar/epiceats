"use strict";

const elementId = (id) => document.getElementById(id);
const className = (classname) => document.getElementsByClassName(classname);
const selector = (selectors) => document.querySelector(selectors);
const allSelector = (selectors) => document.querySelectorAll(selectors);
